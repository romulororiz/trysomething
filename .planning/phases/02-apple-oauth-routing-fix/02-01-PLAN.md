---
phase: 02-apple-oauth-routing-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/vercel.json
autonomous: true
---

<objective>
Add `|apple` to the auth action regex in `server/vercel.json` so `/api/auth/apple` reaches the existing `handleApple()` handler instead of returning 404.

The handler code is complete — only the routing config is broken.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Fix auth route regex in vercel.json</name>
  <files>server/vercel.json</files>
  <action>
    Change `(register|login|refresh|google)` to `(register|login|refresh|google|apple)` in the auth route regex.
  </action>
</task>
</tasks>
