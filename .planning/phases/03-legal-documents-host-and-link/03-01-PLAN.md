---
phase: 03-legal-documents-host-and-link
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/app/terms/page.tsx
  - website/app/privacy/page.tsx
  - website/components/layout/Footer.tsx
autonomous: true
requirements: [COMP-09, COMP-10]

must_haves:
  truths:
    - "Visiting /terms on the deployed website returns a fully rendered Terms of Service page"
    - "Visiting /privacy on the deployed website returns a fully rendered Privacy Policy page"
    - "Both pages load without authentication and display all legal sections"
    - "Footer links point to /terms and /privacy instead of # placeholders"
  artifacts:
    - path: "website/app/terms/page.tsx"
      provides: "Terms of Service static page with all 16 sections"
      contains: "export default function TermsPage"
    - path: "website/app/privacy/page.tsx"
      provides: "Privacy Policy static page with all 11 sections"
      contains: "export default function PrivacyPage"
    - path: "website/components/layout/Footer.tsx"
      provides: "Footer with working legal links"
      contains: "/privacy"
  key_links:
    - from: "website/components/layout/Footer.tsx"
      to: "/terms, /privacy"
      via: "href attributes on anchor tags"
      pattern: "href.*/(terms|privacy)"
    - from: "website/app/terms/page.tsx"
      to: "Next.js static export"
      via: "App Router file-based routing"
      pattern: "export default function TermsPage"
    - from: "website/app/privacy/page.tsx"
      to: "Next.js static export"
      via: "App Router file-based routing"
      pattern: "export default function PrivacyPage"
---

<objective>
Create Terms of Service and Privacy Policy as static pages in the Next.js website, and update the Footer to link to them.

Purpose: App stores require publicly accessible HTTPS URLs for legal documents. The legal content already exists in the Flutter codebase (privacy_policy_screen.dart: 542 lines, terms_of_service_screen.dart: 461 lines). This plan migrates that content to web-hosted pages on the existing Next.js static site.

Output: Two new pages at /terms and /privacy on the deployed website, plus updated Footer links.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-legal-documents-host-and-link/03-RESEARCH.md

Source content (copy text from these into the web pages):
@lib/screens/settings/terms_of_service_screen.dart
@lib/screens/settings/privacy_policy_screen.dart

Existing website patterns:
@website/app/layout.tsx
@website/app/page.tsx
@website/app/globals.css
@website/next.config.ts
@website/components/layout/Footer.tsx

<interfaces>
<!-- Key patterns the executor needs from the existing website -->

From website/app/layout.tsx:
```tsx
// Root layout provides Manrope + Instrument Serif fonts
// and body styling via globals.css
import type { Metadata } from "next";
export const metadata: Metadata = { ... };
export default function RootLayout({ children }: { children: React.ReactNode }) { ... }
```

From website/next.config.ts:
```tsx
// CRITICAL: output: "export" means pages MUST be pure static components
// No cookies(), headers(), searchParams on server, or API routes
const nextConfig: NextConfig = {
  output: "export",
  images: { unoptimized: true },
};
```

From website/app/globals.css (theme tokens available):
```css
--color-bg: #050508;
--color-text-primary: #F0EBE3;
--color-text-secondary: #A09890;
--color-text-muted: #5C5550;
--color-coral: #FF6B6B;
--color-glass: #FFFFFF0F;
--color-glass-border: #FFFFFF1A;
--font-sans: var(--font-manrope);
--font-serif: var(--font-instrument-serif);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Terms of Service and Privacy Policy pages</name>
  <files>website/app/terms/page.tsx, website/app/privacy/page.tsx</files>
  <read_first>
    - lib/screens/settings/terms_of_service_screen.dart (source content — copy ALL text strings from this file)
    - lib/screens/settings/privacy_policy_screen.dart (source content — copy ALL text strings from this file)
    - website/app/layout.tsx (root layout pattern)
    - website/app/globals.css (available theme tokens / CSS classes)
    - website/next.config.ts (confirm output: "export" — no dynamic features allowed)
  </read_first>
  <action>
    Create two new files: `website/app/terms/page.tsx` and `website/app/privacy/page.tsx`.

    CRITICAL CONSTRAINT: The site uses `output: "export"` in next.config.ts. Pages MUST be pure static components. No `cookies()`, `headers()`, `searchParams` on server, no `"use client"` needed (these are static content pages).

    **For both pages, use this structure:**

    1. Export a `metadata` object (Next.js Metadata API):
       - `website/app/terms/page.tsx`: `title: "Terms of Service - TrySomething"`, `description: "Terms of Service for the TrySomething mobile application."`
       - `website/app/privacy/page.tsx`: `title: "Privacy Policy - TrySomething"`, `description: "Privacy Policy for the TrySomething mobile application. Compliant with Swiss FADP and EU GDPR."`

    2. Export a default function component (`TermsPage` / `PrivacyPage`) with this layout:
       ```tsx
       <div className="min-h-screen bg-bg">
         <nav className="max-w-3xl mx-auto px-6 pt-8">
           <a href="/" className="text-sm text-text-secondary hover:text-text-primary transition-colors">
             &larr; Back to TrySomething
           </a>
         </nav>
         <main className="max-w-3xl mx-auto px-6 py-12 pb-24">
           <h1 className="text-3xl font-bold text-text-primary mb-2 font-serif">
             {Title}
           </h1>
           <p className="text-sm text-text-muted mb-10">
             Effective date: 14 March 2026
           </p>
           {/* Sections */}
         </main>
       </div>
       ```

    3. Use these Tailwind classes for content (these all reference theme tokens from globals.css):
       - Section headings (`<h2>`): `text-xl font-semibold text-coral mt-10 mb-4`
       - Subsection headings (`<h3>`): `text-base font-bold text-text-primary mt-6 mb-3`
       - Body paragraphs (`<p>`): `text-text-secondary leading-relaxed mb-4`
       - Bullet lists (`<ul>` + `<li>`): `list-disc list-outside ml-5 space-y-2 text-text-secondary leading-relaxed mb-4` with `marker:text-coral`
       - Contact blocks: A `<div>` with `bg-glass border border-glass-border rounded-xl p-4 mb-4` containing key-value pairs as `<p>` elements with the key in `font-semibold text-text-primary` and value in `text-text-secondary`
       - Data processor blocks (privacy page only): Same glass card styling, with service name bold and details in `text-sm text-text-secondary`

    4. **Content migration — Terms of Service page (16 sections):**
       Copy ALL text content from `terms_of_service_screen.dart`. The sections are:
       - 1. Agreement to Terms
       - 2. Eligibility
       - 3. Your Account (with bullet points)
       - 4. Description of Service (with bullet points)
       - 5. Subscriptions and Payments (5.1 Free Tier, 5.2 TrySomething Pro with bullets, 5.3 Billing, 5.4 Free Trial, 5.5 Cancellation)
       - 6. AI-Generated Content (with bullet points)
       - 7. User Content (7.1 Ownership, 7.2 Responsibilities with bullets)
       - 8. Affiliate Links and Third-Party Services (with bullet list of services)
       - 9. Prohibited Uses (with bullet points)
       - 10. Intellectual Property
       - 11. Limitation of Liability (with bullet points)
       - 12. Indemnification
       - 13. Modifications to Terms
       - 14. Termination
       - 15. Governing Law and Jurisdiction
       - 16. Contact (with contact block: Email: support@trysomething.io, Location: Zurich, Switzerland)

    5. **Content migration — Privacy Policy page (11 sections):**
       Copy ALL text content from `privacy_policy_screen.dart`. The sections are:
       - Intro paragraphs (FADP/GDPR compliance statement)
       - 1. Data Controller (with contact block: Name: Romulo Roriz, Email: support@trysomething.io, Location: Zurich, Switzerland)
       - 2. Data We Collect (2.1 Data You Provide Directly — 9 bullet points, 2.2 Data We Collect Automatically — 4 bullet points, 2.3 Data We Do NOT Collect — 5 bullet points)
       - 3. How We Use Your Data (4 bullet points)
       - 4. AI Data Processing (4.1 Hobby Generation, 4.2 AI Coach with 6 bullets, 4.3 Anthropic's Data Handling)
       - 5. Third-Party Data Processors (10 processor cards: Vercel, Neon, Anthropic, RevenueCat, PostHog, Sentry, Firebase, Unsplash, Google Sign-In, Apple Sign-In — each with service name, purpose, data, location)
       - 6. Data Storage and Security (7 bullet points)
       - 7. Data Retention (8 bullet points)
       - 8. Your Rights (7 bullet points + follow-up paragraph)
       - 9. Children's Privacy (2 paragraphs)
       - 10. Changes to This Privacy Policy (2 paragraphs)
       - 11. Contact (2 contact blocks: support email + FDPIC info)

    DO NOT abbreviate or summarize any legal text. Copy every word exactly as it appears in the Dart source files. The Dart files use `_buildBody('...')`, `_buildBulletPoint('...')`, `_buildSectionTitle('...')`, etc. — extract the string arguments and render them as HTML.

    DO NOT use any dynamic features (no `"use client"`, no `useState`, no `useEffect`). These are purely static pages.
  </action>
  <verify>
    <automated>cd website && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - website/app/terms/page.tsx exists and contains `export default function TermsPage`
    - website/app/terms/page.tsx contains `export const metadata` with `title: "Terms of Service - TrySomething"`
    - website/app/terms/page.tsx contains all 16 section headings: "1. Agreement to Terms" through "16. Contact"
    - website/app/terms/page.tsx contains `support@trysomething.io`
    - website/app/privacy/page.tsx exists and contains `export default function PrivacyPage`
    - website/app/privacy/page.tsx contains `export const metadata` with `title: "Privacy Policy - TrySomething"`
    - website/app/privacy/page.tsx contains all 11 section headings: "1. Data Controller" through "11. Contact"
    - website/app/privacy/page.tsx contains all 10 data processor names: Vercel, Neon, Anthropic, RevenueCat, PostHog, Sentry, Firebase, Unsplash, Google Sign-In, Apple Sign-In
    - website/app/privacy/page.tsx contains `support@trysomething.io` and `edoeb.admin.ch`
    - Neither file contains `"use client"` or `useState` or `useEffect`
    - `cd website && npx next build` succeeds with exit code 0 (confirms static export works)
  </acceptance_criteria>
  <done>
    Both /terms and /privacy pages render all legal content as static HTML. `next build` succeeds with `output: "export"`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Footer legal links from placeholders to real routes</name>
  <files>website/components/layout/Footer.tsx</files>
  <read_first>
    - website/components/layout/Footer.tsx (current state — lines 66-67 have href: "#" placeholders)
  </read_first>
  <action>
    In `website/components/layout/Footer.tsx`, update the legal links array (around lines 65-68) from:
    ```tsx
    { label: "Privacy Policy", href: "#" },
    { label: "Terms of Service", href: "#" },
    ```
    to:
    ```tsx
    { label: "Privacy Policy", href: "/privacy" },
    { label: "Terms of Service", href: "/terms" },
    ```

    No other changes to Footer.tsx are needed. The existing `<a href={link.href}>` rendering pattern will work correctly with these relative paths.
  </action>
  <verify>
    <automated>grep -n "href.*privacy\|href.*terms" website/components/layout/Footer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Footer.tsx contains `href: "/privacy"` (not `href: "#"`)
    - Footer.tsx contains `href: "/terms"` (not `href: "#"`)
    - Footer.tsx does NOT contain any `href: "#"` entries in the legal links section (the Navigate section still uses # anchors, that's fine)
    - `cd website && npx next build` still succeeds
  </acceptance_criteria>
  <done>
    Footer legal links point to /privacy and /terms. Clicking them navigates to the newly created legal pages.
  </done>
</task>

</tasks>

<verification>
1. `cd website && npx next build` completes successfully (static export with 4+ routes: /, /terms, /privacy, /404)
2. `grep -c "export default function TermsPage" website/app/terms/page.tsx` returns 1
3. `grep -c "export default function PrivacyPage" website/app/privacy/page.tsx` returns 1
4. `grep "href.*#" website/components/layout/Footer.tsx` shows NO remaining placeholder legal links
5. `grep -c "Agreement to Terms" website/app/terms/page.tsx` returns at least 1
6. `grep -c "Data Controller" website/app/privacy/page.tsx` returns at least 1
</verification>

<success_criteria>
- Both legal pages build successfully as static HTML via `next build`
- All legal text from the Flutter Dart screens is faithfully migrated to the TSX pages
- Footer links navigate to /terms and /privacy instead of # placeholders
- Pages use the existing warm cinematic theme tokens (bg-bg, text-text-primary, text-coral, etc.)
- Pages include SEO metadata (title, description)
- Pages have a "Back to TrySomething" navigation link
</success_criteria>

<output>
After completion, create `.planning/phases/03-legal-documents-host-and-link/03-01-SUMMARY.md`
</output>
