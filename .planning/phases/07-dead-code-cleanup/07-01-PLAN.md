---
phase: 07-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/screens/features/buddy_mode_screen.dart
  - lib/screens/features/community_stories_screen.dart
  - lib/screens/features/local_discovery_screen.dart
  - lib/screens/features/year_in_review_screen.dart
  - lib/screens/features/weekly_challenge_screen.dart
  - lib/screens/features/mood_match_screen.dart
  - lib/screens/features/seasonal_picks_screen.dart
  - lib/providers/feature_providers.dart
  - lib/data/repositories/social_repository.dart
  - lib/data/repositories/social_repository_api.dart
autonomous: true
requirements:
  - CLEAN-01

must_haves:
  truths:
    - "All 7 hidden screen files are deleted from the repository"
    - "flutter analyze passes with zero errors after deletion"
    - "No active screen has broken imports or missing references"
    - "Orphaned providers (seasonalHobbiesProvider, moodTagsProvider, similarUsersProvider, SimilarUsersNotifier) are removed from feature_providers.dart"
    - "feature_seed_data.dart is untouched (per D-02)"
  artifacts:
    - path: "lib/screens/features/buddy_mode_screen.dart"
      provides: "DELETED"
    - path: "lib/screens/features/community_stories_screen.dart"
      provides: "DELETED"
    - path: "lib/screens/features/local_discovery_screen.dart"
      provides: "DELETED"
    - path: "lib/screens/features/year_in_review_screen.dart"
      provides: "DELETED"
    - path: "lib/screens/features/weekly_challenge_screen.dart"
      provides: "DELETED"
    - path: "lib/screens/features/mood_match_screen.dart"
      provides: "DELETED"
    - path: "lib/screens/features/seasonal_picks_screen.dart"
      provides: "DELETED"
    - path: "lib/providers/feature_providers.dart"
      provides: "Cleaned — orphaned providers removed"
    - path: "lib/data/repositories/social_repository.dart"
      provides: "Cleaned — getSimilarUsers method removed"
    - path: "lib/data/repositories/social_repository_api.dart"
      provides: "Cleaned — getSimilarUsers implementation removed"
  key_links:
    - from: "lib/providers/feature_providers.dart"
      to: "lib/data/repositories/social_repository.dart"
      via: "socialRepositoryProvider import"
      pattern: "socialRepositoryProvider"
    - from: "lib/main.dart"
      to: "lib/providers/feature_providers.dart"
      via: "storiesProvider, buddyProvider, challengeProvider loadFromServer calls"
      pattern: "storiesProvider\\.notifier.*loadFromServer"
    - from: "lib/screens/profile/profile_screen.dart"
      to: "lib/providers/feature_providers.dart"
      via: "achievementsProvider and activityHeatmapProvider"
      pattern: "achievementsProvider|activityHeatmapProvider"
---

<objective>
Delete the 7 hidden feature screens (~2,658 lines) whose routes were already removed, then remove orphaned providers and repository methods that have zero remaining consumers.

Purpose: Reduce codebase size, eliminate dead code that confuses navigation and inflates analysis, and prepare a clean codebase for the Sonnet AI upgrade and app store submission.
Output: 7 screen files deleted, ~3 orphaned providers removed from feature_providers.dart, getSimilarUsers removed from social repository interface and implementation. All active screens remain functional.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dead-code-cleanup/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch-delete the 7 hidden feature screens</name>
  <files>
    lib/screens/features/buddy_mode_screen.dart
    lib/screens/features/community_stories_screen.dart
    lib/screens/features/local_discovery_screen.dart
    lib/screens/features/year_in_review_screen.dart
    lib/screens/features/weekly_challenge_screen.dart
    lib/screens/features/mood_match_screen.dart
    lib/screens/features/seasonal_picks_screen.dart
  </files>
  <read_first>
    lib/screens/features/buddy_mode_screen.dart
    lib/screens/features/community_stories_screen.dart
    lib/screens/features/local_discovery_screen.dart
    lib/screens/features/year_in_review_screen.dart
    lib/screens/features/weekly_challenge_screen.dart
    lib/screens/features/mood_match_screen.dart
    lib/screens/features/seasonal_picks_screen.dart
    lib/router.dart
  </read_first>
  <action>
    Delete all 7 screen files in a single batch (per D-04 and D-06). These files have zero imports across lib/ and no routes in router.dart.

    Files to delete (confirmed zero consumers):
    1. lib/screens/features/buddy_mode_screen.dart (413 lines)
    2. lib/screens/features/community_stories_screen.dart (406 lines)
    3. lib/screens/features/local_discovery_screen.dart (270 lines)
    4. lib/screens/features/year_in_review_screen.dart (344 lines)
    5. lib/screens/features/weekly_challenge_screen.dart (413 lines)
    6. lib/screens/features/mood_match_screen.dart (403 lines)
    7. lib/screens/features/seasonal_picks_screen.dart (409 lines)

    Use `rm` or equivalent to delete each file. Do NOT delete any other files in lib/screens/features/ — files like beginner_faq_screen.dart, cost_calculator_screen.dart, hobby_journal_screen.dart, etc. are live.

    Do NOT touch lib/models/feature_seed_data.dart (per D-02).

    After deletion, run `flutter analyze lib/screens/` to confirm no active screen broke. If flutter analyze reports errors, they will be addressed in Task 2.
  </action>
  <verify>
    <automated>cd D:/programming/projetos/trysomething && ls lib/screens/features/buddy_mode_screen.dart lib/screens/features/community_stories_screen.dart lib/screens/features/local_discovery_screen.dart lib/screens/features/year_in_review_screen.dart lib/screens/features/weekly_challenge_screen.dart lib/screens/features/mood_match_screen.dart lib/screens/features/seasonal_picks_screen.dart 2>&1 | grep -c "No such file"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files no longer exist on disk (ls returns "No such file" for each)
    - No other files in lib/screens/features/ were deleted (beginner_faq_screen.dart, cost_calculator_screen.dart, hobby_journal_screen.dart, etc. still exist)
    - lib/models/feature_seed_data.dart is untouched (per D-02)
  </acceptance_criteria>
  <done>All 7 dead screen files are deleted. ~2,658 lines removed. No live screens were touched.</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned providers and repository methods, verify clean build</name>
  <files>
    lib/providers/feature_providers.dart
    lib/data/repositories/social_repository.dart
    lib/data/repositories/social_repository_api.dart
  </files>
  <read_first>
    lib/providers/feature_providers.dart
    lib/data/repositories/social_repository.dart
    lib/data/repositories/social_repository_api.dart
    lib/models/social.dart
    lib/providers/repository_providers.dart
    lib/main.dart
    lib/screens/profile/profile_screen.dart
  </read_first>
  <action>
    After the 7 screen deletions, these providers/methods have zero remaining consumers. Remove them per D-01 (full cleanup) and D-03 (zero consumers = safe to remove).

    **In lib/providers/feature_providers.dart — REMOVE these sections:**

    1. `seasonalHobbiesProvider` (FutureProvider around line 32-34) — only consumer was seasonal_picks_screen.dart (deleted)
    2. `moodTagsProvider` (FutureProvider around line 36-38) — only consumer was mood_match_screen.dart (deleted)
    3. `SimilarUsersNotifier` class and `similarUsersProvider` (around lines 550-566) — only consumer was local_discovery_screen.dart (deleted)

    **In lib/providers/feature_providers.dart — KEEP these sections (they have live consumers):**
    - `faqProvider`, `costBreakdownProvider`, `budgetAlternativesProvider` — used by live feature screens
    - `ProfileNotifier` + `profileProvider` — used by profile_screen.dart
    - `JournalNotifier` + `journalProvider` — used by hobby_journal_screen.dart
    - `ChallengeNotifier` + `challengeProvider` + `currentChallengeProvider` — used by main.dart, login_screen.dart, register_screen.dart
    - `AchievementsNotifier` + `achievementsProvider` — used by profile_screen.dart
    - `ScheduleNotifier` + `scheduleProvider` — used by hobby_scheduler_screen.dart
    - `ShoppingListNotifier` + `shoppingListCheckedProvider` — used by shopping_list_screen.dart
    - `NotesNotifier` + `notesProvider` — used by personal_notes_screen.dart
    - `selectedCompareProvider` — used by compare_mode_screen.dart
    - `BuddyState` + `BuddyNotifier` + `buddyProvider` — used by main.dart, login_screen.dart, register_screen.dart
    - `StoriesNotifier` + `storiesProvider` — used by main.dart, login_screen.dart, register_screen.dart
    - `combosProvider` — used by hobby_combos_screen.dart
    - `activityLogProvider` + `activityHeatmapProvider` — used by profile_screen.dart

    **In lib/data/repositories/social_repository.dart — REMOVE:**
    - The `getSimilarUsers` method declaration (line 20: `Future<List<NearbyUser>> getSimilarUsers({String? hobbyId});`)
    - Keep the `// Similar users` comment removal or keep — your discretion.

    **In lib/data/repositories/social_repository_api.dart — REMOVE:**
    - The entire `getSimilarUsers` implementation (lines 103-117, from the `// Similar users` comment through the closing brace)

    **DO NOT remove from social_repository.dart or social_repository_api.dart:**
    - Stories methods (getStories, createStory, deleteStory, addReaction, removeReaction) — live consumers
    - Buddies methods (getBuddiesWithActivity, getBuddyRequests, sendBuddyRequest, respondToRequest, cancelRequest) — live consumers

    **DO NOT remove:**
    - The `import '../models/social.dart';` from feature_providers.dart — still needed by BuddyState/StoriesNotifier
    - The `import '../data/repositories/social_repository.dart';` from feature_providers.dart — still needed by BuddyNotifier/StoriesNotifier
    - Any models in social.dart (JournalEntry, BuddyProfile, BuddyActivity, CommunityStory, NearbyUser, BuddyRequest) — NearbyUser is still referenced by feature_seed_data.dart (per D-02), and other types have live consumers
    - The `import '../models/gamification.dart';` from feature_providers.dart — still needed by ChallengeNotifier/AchievementsNotifier
    - lib/models/feature_seed_data.dart (per D-02)

    **After edits, run `flutter analyze` on the full lib/ directory (per D-05).** If any errors appear (orphaned imports, missing references), fix them before considering this task done. Likely issues:
    - If social_repository_api.dart has an unused import of NearbyUser after removing getSimilarUsers, the import of social.dart may show unused-import warning — but it's still needed for BuddyProfile, etc., so it should be fine.
    - Check if the `NearbyUser` import in social_repository.dart is still needed after removing the method — it's used in the return type. After removal, if `NearbyUser` is no longer referenced in the abstract class, the import may become unused. But social.dart is imported as a whole file, and other types (BuddyRequest, etc.) are still used.

    After flutter analyze passes, run `git diff --stat` to confirm the total line reduction.
  </action>
  <verify>
    <automated>cd D:/programming/projetos/trysomething && dart analyze lib/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` (or `dart analyze lib/`) passes with zero errors
    - grep for "seasonalHobbiesProvider" in lib/providers/feature_providers.dart returns zero matches
    - grep for "moodTagsProvider" in lib/providers/feature_providers.dart returns zero matches
    - grep for "SimilarUsersNotifier" in lib/providers/feature_providers.dart returns zero matches
    - grep for "similarUsersProvider" in lib/providers/feature_providers.dart returns zero matches
    - grep for "getSimilarUsers" in lib/data/repositories/social_repository.dart returns zero matches
    - grep for "getSimilarUsers" in lib/data/repositories/social_repository_api.dart returns zero matches
    - lib/models/feature_seed_data.dart is unchanged (per D-02) — verify with `git diff lib/models/feature_seed_data.dart` showing no changes
    - buddyProvider, storiesProvider, challengeProvider, achievementsProvider still exist in feature_providers.dart
    - lib/main.dart still compiles (loadFromServer calls for stories/buddy/challenge untouched)
    - lib/screens/profile/profile_screen.dart still compiles (achievementsProvider and activityHeatmapProvider untouched)
  </acceptance_criteria>
  <done>Orphaned providers and repository methods removed. flutter analyze passes with zero errors. All active screens (Home, Discover, You, Detail, Session, Coach, Settings, Profile) remain functional.</done>
</task>

</tasks>

<verification>
1. All 7 screen files deleted — `ls lib/screens/features/{buddy_mode,community_stories,local_discovery,year_in_review,weekly_challenge,mood_match,seasonal_picks}_screen.dart` returns "No such file" for all
2. `dart analyze lib/` passes with zero errors
3. `git diff --stat` shows approximately 2,700+ lines removed (2,658 from screens + orphaned providers/methods)
4. `lib/models/feature_seed_data.dart` has zero changes in git diff
5. Active screens verified by grepping for their imports: `grep -r "buddy_mode_screen\|community_stories_screen\|local_discovery_screen\|year_in_review_screen\|weekly_challenge_screen\|mood_match_screen\|seasonal_picks_screen" lib/` returns zero matches
</verification>

<success_criteria>
- 7 hidden screen files completely removed from repository
- flutter analyze passes with zero errors
- No active screen has broken imports or missing references
- Orphaned providers (seasonalHobbiesProvider, moodTagsProvider, similarUsersProvider) removed
- Orphaned repository method (getSimilarUsers) removed from interface and implementation
- feature_seed_data.dart untouched per D-02
- git diff stat confirms ~2,700+ lines removed
</success_criteria>

<output>
After completion, create `.planning/phases/07-dead-code-cleanup/07-01-SUMMARY.md`
</output>
