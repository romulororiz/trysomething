---
phase: 09-app-store-assets-and-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/Runner/PrivacyInfo.xcprivacy
  - ios/Runner.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - COMP-12

must_haves:
  truths:
    - "App-level PrivacyInfo.xcprivacy declares UserDefaults access with reason CA92.1"
    - "TARGETED_DEVICE_FAMILY is '1' (iPhone only) in all three build configurations"
    - "PrivacyInfo.xcprivacy is registered in the Xcode project as a build resource"
  artifacts:
    - path: "ios/Runner/PrivacyInfo.xcprivacy"
      provides: "App-level Apple Privacy Manifest declaring UserDefaults API access"
      contains: "NSPrivacyAccessedAPICategoryUserDefaults"
    - path: "ios/Runner.xcodeproj/project.pbxproj"
      provides: "Xcode project with privacy manifest reference and iPhone-only targeting"
      contains: "TARGETED_DEVICE_FAMILY = \"1\""
  key_links:
    - from: "ios/Runner.xcodeproj/project.pbxproj"
      to: "ios/Runner/PrivacyInfo.xcprivacy"
      via: "PBXFileReference + PBXResourcesBuildPhase entry"
      pattern: "PrivacyInfo\\.xcprivacy"
---

<objective>
Add the app-level Apple Privacy Manifest and change device targeting to iPhone-only.

Purpose: COMP-12 requires privacy manifests for all SDKs. Research confirmed Firebase FCM, PostHog, Sentry, and RevenueCat bundle their own manifests. The app itself uses UserDefaults (via Hive and SharedPreferences) and needs its own declaration. Additionally, per D-03/D-04, TARGETED_DEVICE_FAMILY must change from "1,2" to "1" to avoid iPad screenshot requirements.

Output: `PrivacyInfo.xcprivacy` file created, registered in Xcode project, device family set to iPhone-only.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-app-store-assets-and-admin/09-CONTEXT.md
@.planning/phases/09-app-store-assets-and-admin/09-RESEARCH.md

@ios/Runner.xcodeproj/project.pbxproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PrivacyInfo.xcprivacy and register in Xcode project</name>
  <files>ios/Runner/PrivacyInfo.xcprivacy, ios/Runner.xcodeproj/project.pbxproj</files>
  <read_first>
    - ios/Runner.xcodeproj/project.pbxproj (full file — need PBXBuildFile, PBXFileReference, PBXGroup Runner children, PBXResourcesBuildPhase Runner resources sections)
  </read_first>
  <action>
**Step 1 — Create `ios/Runner/PrivacyInfo.xcprivacy`** with this exact content (per D-01, D-02, research template):

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>NSPrivacyTracking</key>
    <false/>
    <key>NSPrivacyTrackingDomains</key>
    <array/>
    <key>NSPrivacyCollectedDataTypes</key>
    <array/>
    <key>NSPrivacyAccessedAPITypes</key>
    <array>
        <dict>
            <key>NSPrivacyAccessedAPIType</key>
            <string>NSPrivacyAccessedAPICategoryUserDefaults</string>
            <key>NSPrivacyAccessedAPITypeReasons</key>
            <array>
                <string>CA92.1</string>
            </array>
        </dict>
    </array>
</dict>
</plist>
```

Reason code CA92.1 = "Access info from the same app that wrote it" — covers Hive and SharedPreferences reading their own keys.

**Step 2 — Register PrivacyInfo.xcprivacy in `project.pbxproj`:**

Generate a unique 24-char hex ID for the file reference (e.g., `78A1B2C3D4E5F60700A1B2C3`) and another for the build file entry (e.g., `78A1B2C3D4E5F60800A1B2C3`). Use IDs that do not conflict with existing entries in the file.

Add three entries to `project.pbxproj`:

1. **PBXBuildFile section** (after the last existing entry, before `/* End PBXBuildFile section */`):
   ```
   		{BUILD_FILE_ID} /* PrivacyInfo.xcprivacy in Resources */ = {isa = PBXBuildFile; fileRef = {FILE_REF_ID} /* PrivacyInfo.xcprivacy */; };
   ```

2. **PBXFileReference section** (after the last existing entry, before `/* End PBXFileReference section */`):
   ```
   		{FILE_REF_ID} /* PrivacyInfo.xcprivacy */ = {isa = PBXFileReference; lastKnownFileType = text.xml; path = PrivacyInfo.xcprivacy; sourceTree = "<group>"; };
   ```

3. **PBXGroup — Runner children** (at line ~113, add to the `97C146F01CF9000F007C117D /* Runner */` group's children array, after `97C147021CF9000F007C117D /* Info.plist */`):
   ```
   				{FILE_REF_ID} /* PrivacyInfo.xcprivacy */,
   ```

4. **PBXResourcesBuildPhase — Runner Resources** (at line ~217, add to `97C146EC1CF9000F007C117D /* Resources */` files array, after the last existing entry):
   ```
   				{BUILD_FILE_ID} /* PrivacyInfo.xcprivacy in Resources */,
   ```
  </action>
  <verify>
    <automated>grep -c "PrivacyInfo.xcprivacy" ios/Runner.xcodeproj/project.pbxproj</automated>
  </verify>
  <acceptance_criteria>
    - File `ios/Runner/PrivacyInfo.xcprivacy` exists and contains `NSPrivacyAccessedAPICategoryUserDefaults`
    - File `ios/Runner/PrivacyInfo.xcprivacy` contains reason code `CA92.1`
    - `project.pbxproj` contains at least 4 references to `PrivacyInfo.xcprivacy` (PBXBuildFile, PBXFileReference, PBXGroup child, PBXResourcesBuildPhase entry)
    - `NSPrivacyTracking` is set to `false` (app does not track per ATT)
    - `NSPrivacyCollectedDataTypes` is empty array (data collection declared in App Store Connect, not in manifest)
  </acceptance_criteria>
  <done>PrivacyInfo.xcprivacy created with UserDefaults CA92.1 declaration and fully registered in the Xcode project build resources</done>
</task>

<task type="auto">
  <name>Task 2: Change TARGETED_DEVICE_FAMILY to iPhone-only</name>
  <files>ios/Runner.xcodeproj/project.pbxproj</files>
  <read_first>
    - ios/Runner.xcodeproj/project.pbxproj (lines 350-360, 475-490, 530-540 — the three TARGETED_DEVICE_FAMILY occurrences)
  </read_first>
  <action>
Per D-03 and D-04, change `TARGETED_DEVICE_FAMILY` from `"1,2"` (iPhone + iPad) to `"1"` (iPhone only) at all three occurrences in `ios/Runner.xcodeproj/project.pbxproj`:

- **Line 357** (Profile build configuration): `TARGETED_DEVICE_FAMILY = "1,2";` -> `TARGETED_DEVICE_FAMILY = "1";`
- **Line 483** (Debug build configuration): `TARGETED_DEVICE_FAMILY = "1,2";` -> `TARGETED_DEVICE_FAMILY = "1";`
- **Line 536** (Release build configuration): `TARGETED_DEVICE_FAMILY = "1,2";` -> `TARGETED_DEVICE_FAMILY = "1";`

All three MUST be changed. Missing one means iPad screenshots are still required for that build config. Use Edit tool for surgical replacement — do not rewrite the entire file.
  </action>
  <verify>
    <automated>grep -c 'TARGETED_DEVICE_FAMILY = "1";' ios/Runner.xcodeproj/project.pbxproj | grep -q "3" && echo "PASS: 3 occurrences of iPhone-only" || echo "FAIL: expected exactly 3 occurrences"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'TARGETED_DEVICE_FAMILY = "1,2"' ios/Runner.xcodeproj/project.pbxproj` returns zero matches
    - `grep -c 'TARGETED_DEVICE_FAMILY = "1";' ios/Runner.xcodeproj/project.pbxproj` returns exactly 3
    - No other lines in the file were changed
  </acceptance_criteria>
  <done>All three build configurations (Debug, Profile, Release) target iPhone only — no iPad screenshots required for App Store submission</done>
</task>

</tasks>

<verification>
- `ios/Runner/PrivacyInfo.xcprivacy` exists and is valid XML containing `NSPrivacyAccessedAPICategoryUserDefaults` with reason `CA92.1`
- `ios/Runner.xcodeproj/project.pbxproj` references `PrivacyInfo.xcprivacy` in at least 4 places
- All 3 `TARGETED_DEVICE_FAMILY` entries are `"1"` (zero occurrences of `"1,2"`)
- `flutter analyze lib/` passes (no Dart code changed, but confirms project is still valid)
</verification>

<success_criteria>
- Privacy manifest file created and registered per COMP-12
- Device family set to iPhone-only per D-03/D-04
- SDK-bundled manifests (Firebase, PostHog, Sentry, RevenueCat) verified present by research — D-02 IPA build test is a user-side verification on macOS
</success_criteria>

<output>
After completion, create `.planning/phases/09-app-store-assets-and-admin/09-01-SUMMARY.md`
</output>
