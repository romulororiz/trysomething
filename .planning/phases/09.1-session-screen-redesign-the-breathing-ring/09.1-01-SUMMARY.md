---
phase: 09.1-session-screen-redesign-the-breathing-ring
plan: 01
subsystem: session-screen
tags: [components, animation, data-plumbing, dead-code-cleanup]
dependency_graph:
  requires: []
  provides: [BreathingRing, BreathingRingPainter, FilmGrainOverlay, coachTip-pipeline]
  affects: [session_screen, session_provider, home_screen, router]
tech_stack:
  added: []
  patterns: [CustomPainter-layered-ring, sinusoidal-breathing-animation, milestone-pulse]
key_files:
  created:
    - lib/components/breathing_ring.dart
    - lib/components/film_grain_overlay.dart
    - assets/textures/film_grain.png
  modified:
    - lib/models/session.dart
    - lib/models/session.freezed.dart
    - lib/providers/session_provider.dart
    - lib/screens/session/session_screen.dart
    - lib/router.dart
    - lib/screens/home/home_screen.dart
    - pubspec.yaml
    - test/widget/screens/session_timer_test.dart
    - test/unit/providers/session_provider_test.dart
  deleted:
    - lib/components/particle_timer_painter.dart
    - lib/components/category_shape_painter.dart
    - lib/components/brushstroke_timer_painter.dart
    - lib/components/session_glow_widget.dart
decisions:
  - "BreathingRingPainter draws all 4 layers in single CustomPainter for efficiency"
  - "Film grain as static PNG overlay (not per-frame CustomPainter noise) for zero GPU cost"
  - "SessionParticleField replaced with Container placeholder pending Plan 02 ring integration"
  - "Test helpers updated for both completionMessage and coachTip params (Pitfall 6)"
metrics:
  duration: 6min
  completed: "2026-03-22"
  tasks_completed: 2
  tasks_total: 2
  lines_added: ~266
  lines_deleted: ~1028
---

# Phase 09.1 Plan 01: Breathing Ring Components & Data Plumbing Summary

**One-liner:** 4-layer CustomPainter ring with sinusoidal breathing + film grain overlay + coachTip wired end-to-end through 6 layers + 1015 lines of dead particle code deleted.

## What Was Done

### Task 1: BreathingRing + FilmGrainOverlay + Film Grain PNG

Created `lib/components/breathing_ring.dart` with:
- `BreathingRingPainter` CustomPainter drawing 4 layers: track circle (6% white), glow halo (MaskFilter.blur), progress arc (coral), leading dot with soft glow
- `BreathingRing` StatefulWidget with `_breathController` (configurable cycle duration, .repeat()) and `_pulseController` (600ms milestone pulse)
- Breathing scale: `1.0 + 0.008 * sin(value * 2 * pi)` per D-10
- Proper `shouldRepaint` comparing all 4 fields (progress, breathScale, glowIntensity, ringOpacity)
- `triggerMilestonePulse()` public method for D-23 halfway celebration
- `didUpdateWidget` handles duration changes per Pitfall 3 (stop, update, restart)
- Wrapped in `RepaintBoundary` per Pitfall 2

Created `lib/components/film_grain_overlay.dart` with:
- `FilmGrainOverlay` stateless widget using `Positioned.fill` + `IgnorePointer`
- Tiles 256x256 PNG at 1.5% opacity with `FilterQuality.none`

Generated `assets/textures/film_grain.png`:
- 256x256 grayscale noise texture (96KB)
- Added `assets/textures/` to pubspec.yaml assets list

**Commit:** `11bd457`

### Task 2: coachTip Pipeline + Dead File Deletion

Wired `coachTip` through all 6 touch points following the `completionMessage` pattern:
1. `lib/models/session.dart` - Added `String? coachTip` to SessionState factory
2. `lib/models/session.freezed.dart` - Regenerated via build_runner (19 references)
3. `lib/providers/session_provider.dart` - Added coachTip param to startSession()
4. `lib/screens/session/session_screen.dart` - Field, constructor, route(), initState
5. `lib/router.dart` - Passes `extra['coachTip']` to SessionScreen
6. `lib/screens/home/home_screen.dart` - All 3 launch sites pass `step.coachTip`

Deleted 4 dead component files (~1015 lines):
- `particle_timer_painter.dart` (644 lines - SessionParticleField, _ParticleField3DPainter)
- `category_shape_painter.dart` (217 lines - zero imports)
- `brushstroke_timer_painter.dart` (119 lines - zero imports)
- `session_glow_widget.dart` (35 lines - zero imports)

Kept `radial_hold_painter.dart` (used for check-in completion mode).

Replaced `SessionParticleField` in session_screen.dart with `Container(color: AppColors.background)` placeholder (Plan 02 will replace with 5-layer stack).

Updated test helpers (Pitfall 6):
- `_NullSessionNotifier.startSession` in widget test
- `_startSession` helper in unit test

**Commit:** `152b5f3`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated test files for new parameters (Pitfall 6)**
- **Found during:** Task 2
- **Issue:** `_NullSessionNotifier.startSession` in widget test was missing `completionMessage` parameter (pre-existing) AND the new `coachTip`. Unit test `_startSession` helper also missing both.
- **Fix:** Added `completionMessage` and `coachTip` optional params to both test helpers
- **Files modified:** `test/widget/screens/session_timer_test.dart`, `test/unit/providers/session_provider_test.dart`
- **Commit:** `152b5f3`

## Verification Results

- `dart analyze` on all modified files: 0 errors (2 pre-existing warnings only)
- 4 dead files confirmed deleted (ls returns "No such file or directory")
- `radial_hold_painter.dart` confirmed still present
- `coachTip` found in `session.dart` model
- 3 occurrences of `'coachTip': step.coachTip` in `home_screen.dart`
- Film grain PNG: valid 256x256 RGB PNG

## Known Stubs

**1. Session screen background placeholder**
- File: `lib/screens/session/session_screen.dart`, line ~163
- Code: `Container(color: AppColors.background)` replacing former `SessionParticleField`
- Reason: Intentional placeholder. Plan 02 will replace with the 5-layer stack (breathing background, ambient gradient, film grain, breathing ring, phase content).

## Self-Check: PASSED
