---
phase: 09.1-session-screen-redesign-the-breathing-ring
plan: 03
subsystem: session-screen
tags: [session-phases, ring-layout, completionMessage, tests]
dependency_graph:
  requires: [BreathingRing, 5-layer-cinematic-stack, coachTip-pipeline]
  provides: [ring-centric-prepare-phase, completionMessage-display, ring-centric-complete-phase, updated-test-suite]
  affects: [session_prepare_phase, session_complete_phase, session_reflect_phase, session_timer_test]
tech_stack:
  added: []
  patterns: [flex-ratio-ring-clearing, conditional-animation-delay]
key_files:
  created: []
  modified:
    - lib/screens/session/session_prepare_phase.dart
    - lib/screens/session/session_complete_phase.dart
    - lib/screens/session/session_reflect_phase.dart
    - test/widget/screens/session_timer_test.dart
decisions:
  - "Spacer flex 1:3 ratio in prepare phase clears ring area in the vertical middle"
  - "Spacer flex 3:2 ratio in complete phase pushes text below the ring"
  - "completionMessage animation delay bumps next step preview by +100ms when present"
  - "Coach tip test uses Container shape/size predicate rather than Phosphor icon finder for robustness"
metrics:
  duration: 4min
  completed: "2026-03-22"
  tasks_completed: 2
  tasks_total: 2
  lines_added: ~60
  lines_deleted: ~8
---

# Phase 09.1 Plan 03: Prepare/Complete/Reflect Phase Layouts & Test Suite Summary

**One-liner:** Three non-timer phases redesigned with flex-ratio layouts that clear the ring area, completionMessage display added to complete phase, and 3 new tests covering BreathingRing presence and coach tip visibility (18/18 pass).

## What Was Done

### Task 1: Update prepare, complete, and reflect phase layouts for ring-centric design

**Prepare phase (`session_prepare_phase.dart`):**
- Top `Spacer(flex: 2)` changed to `Spacer(flex: 1)` -- pushes hobby info toward top
- Middle `SizedBox(height: 48)` changed to `Spacer(flex: 3)` -- leaves ample vertical space for the ring track circle to show through
- Bottom `Spacer(flex: 2)` removed, replaced with `SizedBox(height: 24)` -- duration selector and CTA sit at screen bottom, below ring area
- `_DurationSelector` and `_CoralButton` widgets completely unchanged
- All flutter_animate stagger animations preserved

**Complete phase (`session_complete_phase.dart`):**
- Top `Spacer(flex: 2)` changed to `Spacer(flex: 3)` -- pushes "STEP COMPLETE" text below the full ring
- Added `completionMessage` display (D-13): shows `session.completionMessage` when non-null, styled with `AppTypography.body` in `textSecondary`, centered, with 300ms fade-in at 800ms delay
- Next step preview delay bumped from 900ms to 1000ms when completionMessage is present (smoother stagger)
- Bottom `Spacer(flex: 3)` changed to `Spacer(flex: 2)` -- rebalances after top flex increase
- 3-second auto-exit timer preserved (D-14)
- NO journey progress bar (D-15)

**Reflect phase (`session_reflect_phase.dart`):**
- Top `SizedBox(height: 64)` reduced to `SizedBox(height: 48)` -- brings "How was that?" 16px closer to the ring at 100%
- No other changes -- the reflect phase layout was already well-designed

**Commit:** `e255a73`

### Task 2: Update session_timer_test.dart for new session structure

- Added `import 'package:trysomething/components/breathing_ring.dart';`
- Added `coachTip` parameter to `_pumpScreen` helper, passed through to `SessionScreen`
- Added 3 new tests:
  1. `renders BreathingRing widget instead of particle field` -- confirms `BreathingRing` is in the widget tree during timer phase
  2. `shows coach tip button when coachTip is provided` -- verifies the 36dp circle Container with `BoxShape.circle` exists when coachTip is passed
  3. `hides coach tip button when coachTip is null` -- confirms "Coach Tip" text is not in the tree when no coachTip
- All 15 existing tests continue to pass unchanged
- Final count: 18/18 tests pass

**Commit:** `f7f2946`

## Deviations from Plan

None - plan executed exactly as written.

## Verification Results

- `dart analyze lib/screens/session/ --no-fatal-warnings` -- 0 errors (1 pre-existing info-level issue in session_screen.dart)
- `flutter test test/widget/screens/session_timer_test.dart` -- 18/18 pass, 0 failures
- `completionMessage` found at 4 locations in session_complete_phase.dart (2 conditionals, 1 text, 1 delay check)
- `coachTip` found at 7 locations in session_timer_test.dart (param, helper, constructor, 2 test names, 2 test bodies)

## Known Stubs

None - all data pipelines fully wired. `completionMessage` was already on `SessionState` from Plan 01; this plan wired it to the UI. `coachTip` was already wired end-to-end in Plan 01/02.

## Self-Check: PASSED
