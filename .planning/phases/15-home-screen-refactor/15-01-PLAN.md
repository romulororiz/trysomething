---
phase: 15-home-screen-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/screens/home/home_screen.dart
  - lib/screens/home/paused_hobby_page.dart
  - lib/screens/home/home_roadmap_section.dart
autonomous: true
requirements:
  - HOME-02
  - HOME-05

must_haves:
  truths:
    - "Paused hobby page renders identically — blurred image, PAUSED chip, title, days counter, Resume CTA, view details link"
    - "Roadmap journey renders identically — progress bar, step items with focus/complete/future states, coach tips, start session CTAs"
    - "Home tab navigates and behaves identically — PageView swipe, page dots, loading states, empty state all unchanged"
  artifacts:
    - path: "lib/screens/home/paused_hobby_page.dart"
      provides: "PausedHobbyPage widget extracted from _PausedHobbyPage"
      contains: "class PausedHobbyPage"
    - path: "lib/screens/home/home_roadmap_section.dart"
      provides: "RoadmapJourney and StepItem widgets extracted from _RoadmapJourney and _StepItem"
      contains: "class RoadmapJourney"
    - path: "lib/screens/home/home_screen.dart"
      provides: "Home screen shell now importing extracted widgets"
  key_links:
    - from: "lib/screens/home/home_screen.dart"
      to: "lib/screens/home/paused_hobby_page.dart"
      via: "import and PausedHobbyPage constructor call"
      pattern: "PausedHobbyPage\\("
    - from: "lib/screens/home/home_screen.dart"
      to: "lib/screens/home/home_roadmap_section.dart"
      via: "import — RoadmapJourney used inside _HobbyPageContent.build()"
      pattern: "RoadmapJourney\\("
---

<objective>
Extract the paused hobby page and roadmap journey widgets from home_screen.dart into standalone files.

Purpose: First extraction pass — move the two largest leaf widget groups (paused page ~135 lines, roadmap ~1091 lines) out of home_screen.dart. These are self-contained and do not reference each other, making them safe to extract in parallel within one task.

Output: Two new files (`paused_hobby_page.dart`, `home_roadmap_section.dart`) and a slimmed home_screen.dart that imports them.
</objective>

<execution_context>
@C:/Users/rmulororiz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rmulororiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/screens/home/home_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PausedHobbyPage and RoadmapJourney into standalone files</name>
  <files>lib/screens/home/paused_hobby_page.dart, lib/screens/home/home_roadmap_section.dart, lib/screens/home/home_screen.dart</files>
  <action>
This is a PURE REFACTOR. Zero logic changes, zero styling changes. Move code exactly as-is, only changing visibility (remove underscore prefix to make classes public).

**Step A — Create `lib/screens/home/paused_hobby_page.dart`:**
1. Move `_PausedHobbyPage` (lines 356-490) into new file as `PausedHobbyPage` (public class).
2. Change class name from `_PausedHobbyPage` to `PausedHobbyPage`. Remove underscore from constructor.
3. Copy all necessary imports from home_screen.dart that this widget uses:
   - `dart:ui` (for ImageFilter)
   - `package:flutter/material.dart`
   - `package:flutter_riverpod/flutter_riverpod.dart`
   - `package:go_router/go_router.dart`
   - `package:cached_network_image/cached_network_image.dart`
   - `../../components/logo_loader.dart`
   - `../../models/hobby.dart`
   - `../../providers/hobby_provider.dart`
   - `../../providers/user_provider.dart`
   - `../../theme/app_colors.dart`
   - `../../theme/app_typography.dart`
4. Do NOT change any widget tree, colors, padding, text, or behavior.

**Step B — Create `lib/screens/home/home_roadmap_section.dart`:**
1. Move `_RoadmapJourney` + `_RoadmapJourneyState` (lines 1268-1641) and `_StepItem` (lines 1659-2375) into new file.
2. Also move the teal color constants (lines 1268-1274): `_tealBg`, `_tealBgActive`, `_tealBorder`, `_tealBorderActive`, `_tealText`, `_tealTextBright`, `_tealBar`.
3. Rename `_RoadmapJourney` to `RoadmapJourney` (public). Keep `_RoadmapJourneyState` private.
4. Rename `_StepItem` to `StepItem` (public, since it's used by RoadmapJourney in the same file — actually keep it private `_StepItem` since it's only used within this file). Actually: keep `_StepItem` as-is (private) since it stays in the same file as `RoadmapJourney`.
5. Copy all necessary imports:
   - `package:flutter/material.dart`
   - `package:flutter/services.dart` (for HapticFeedback)
   - `package:flutter_riverpod/flutter_riverpod.dart`
   - `package:go_router/go_router.dart`
   - `package:flutter_animate/flutter_animate.dart`
   - `../../router.dart` show `rootNavigatorKey`
   - `../session/hobby_completion_screen.dart`
   - `../../components/app_overlays.dart` (for showAppSheet)
   - `../../models/hobby.dart`
   - `../../providers/user_provider.dart`
   - `../../theme/app_colors.dart`
   - `../../theme/app_typography.dart`
   - `../../theme/spacing.dart`
6. Include the two unused methods `_buildExpandedCard` and `_buildCompactRow` (lines 2116-2374) — move them as-is to preserve exact code. They have `// ignore: unused_element` annotations.
7. Do NOT change any widget tree, colors, padding, text, or behavior.

**Step C — Update `lib/screens/home/home_screen.dart`:**
1. Remove the extracted code blocks:
   - Lines 352-490 (`_PausedHobbyPage`)
   - Lines 1268-2375 (teal constants, `_RoadmapJourney`, `_StepItem`)
2. Add imports for the new files:
   - `import 'paused_hobby_page.dart';`
   - `import 'home_roadmap_section.dart';`
3. Update references:
   - `_PausedHobbyPage(` -> `PausedHobbyPage(`
   - `_RoadmapJourney(` -> `RoadmapJourney(`
4. Remove imports that are no longer used by home_screen.dart ONLY if they were exclusively used by the extracted widgets. Be conservative — if unsure, leave the import.
5. Specifically check: `dart:ui` may no longer be needed (was only used by PausedHobbyPage's ImageFilter). `package:flutter/services.dart` may no longer be needed (was only used by HapticFeedback in roadmap). `package:flutter_animate/flutter_animate.dart` may no longer be needed (was only used by StepItem). `../../router.dart` may no longer be needed (was used by roadmap for rootNavigatorKey). `../session/hobby_completion_screen.dart` may no longer be needed (was used by roadmap).
  </action>
  <verify>
    <automated>cd /c/dev/trysomething && dart analyze lib/screens/home/paused_hobby_page.dart lib/screens/home/home_roadmap_section.dart lib/screens/home/home_screen.dart</automated>
  </verify>
  <done>
    - `paused_hobby_page.dart` exists with public `PausedHobbyPage` class, identical widget tree to the original `_PausedHobbyPage`
    - `home_roadmap_section.dart` exists with public `RoadmapJourney` class and private `_StepItem`, identical widget trees
    - `home_screen.dart` imports both new files and references `PausedHobbyPage` and `RoadmapJourney`
    - `dart analyze` passes with 0 errors, 0 warnings on all three files
    - No logic, styling, or behavioral changes — pure file extraction
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify home_screen.dart line count and run full analysis</name>
  <files>lib/screens/home/home_screen.dart</files>
  <action>
1. Run `wc -l lib/screens/home/home_screen.dart` to confirm the file has shrunk significantly (should drop from 2,375 to approximately 1,150 lines after removing ~1,225 lines of paused page + roadmap code).
2. Run `dart analyze lib/screens/home/` to verify the entire home directory passes with 0 errors, 0 warnings.
3. If any analyze errors exist, fix import issues (missing imports in extracted files, or stale imports in home_screen.dart).
4. Do NOT change any widget tree, colors, padding, text, or behavior — only fix import/reference issues if any exist.
  </action>
  <verify>
    <automated>cd /c/dev/trysomething && dart analyze lib/screens/home/ && wc -l lib/screens/home/*.dart</automated>
  </verify>
  <done>
    - `dart analyze lib/screens/home/` passes with 0 errors, 0 warnings
    - home_screen.dart is approximately 1,100-1,200 lines (reduced from 2,375, with the remaining ~600 lines to be extracted in Plan 02)
    - All three files in `lib/screens/home/` have valid Dart syntax and correct imports
  </done>
</task>

</tasks>

<verification>
1. `dart analyze lib/screens/home/` — 0 errors, 0 warnings
2. `wc -l lib/screens/home/home_screen.dart` — file is significantly smaller than 2,375 lines
3. All extracted widgets maintain identical class structure and widget trees
</verification>

<success_criteria>
- PausedHobbyPage is a standalone public widget in its own file (HOME-02)
- RoadmapJourney and StepItem are standalone widgets in their own file (HOME-05)
- home_screen.dart imports and uses the extracted widgets
- dart analyze passes with 0 errors, 0 warnings on all files
- Zero visual or behavioral changes
</success_criteria>

<output>
After completion, create `.planning/phases/15-home-screen-refactor/15-01-SUMMARY.md`
</output>
