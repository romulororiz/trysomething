---
phase: 15-home-screen-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - lib/screens/home/home_screen.dart
  - lib/screens/home/active_hobby_page.dart
  - lib/screens/home/home_journal_section.dart
autonomous: true
requirements:
  - HOME-01
  - HOME-03
  - HOME-04

must_haves:
  truths:
    - "Active hobby page renders identically — hero image, title, streak badge, 3-dot menu, restart card, roadmap, schedule, coach entry, starter kit, quick links, journal section"
    - "Journal entry tiles render identically — photo thumbnail or icon, text preview, date label, tap handler"
    - "Empty journal state shows 'No entries yet' text identically"
    - "home_screen.dart is under 500 lines and contains only the top-level scaffold, PageView switching, loading/empty states, and widget composition"
  artifacts:
    - path: "lib/screens/home/active_hobby_page.dart"
      provides: "ActiveHobbyPage widget (formerly _HobbyPageContent) plus RestartCard helper"
      contains: "class ActiveHobbyPage"
    - path: "lib/screens/home/home_journal_section.dart"
      provides: "JournalEntryTile widget and HomeJournalSection widget for journal preview"
      contains: "class JournalEntryTile"
    - path: "lib/screens/home/home_screen.dart"
      provides: "Thin shell under 500 lines composing all extracted widgets"
  key_links:
    - from: "lib/screens/home/home_screen.dart"
      to: "lib/screens/home/active_hobby_page.dart"
      via: "import and ActiveHobbyPage constructor call in _HobbyPage"
      pattern: "ActiveHobbyPage\\("
    - from: "lib/screens/home/active_hobby_page.dart"
      to: "lib/screens/home/home_journal_section.dart"
      via: "import and JournalEntryTile usage in journal section"
      pattern: "JournalEntryTile\\("
    - from: "lib/screens/home/active_hobby_page.dart"
      to: "lib/screens/home/home_roadmap_section.dart"
      via: "import and RoadmapJourney usage (extracted in Plan 01)"
      pattern: "RoadmapJourney\\("
---

<objective>
Extract the active hobby page content and journal entry tiles from home_screen.dart, completing the refactor to under 500 lines.

Purpose: Final extraction pass — move `_HobbyPageContent` (~560 lines + `_RestartCard` ~74 lines) and `_JournalEntryTile` (~71 lines) into standalone files. After this, home_screen.dart will be a thin shell under 500 lines containing only the PageView coordinator, loading states, and empty state.

Output: Two new files (`active_hobby_page.dart`, `home_journal_section.dart`) and a final home_screen.dart under 500 lines.
</objective>

<execution_context>
@C:/Users/rmulororiz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rmulororiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-home-screen-refactor/15-01-SUMMARY.md
@lib/screens/home/home_screen.dart
@lib/screens/home/home_roadmap_section.dart
</context>

<interfaces>
<!-- From Plan 01 output — executor needs these to wire imports correctly -->

From lib/screens/home/home_roadmap_section.dart:
```dart
class RoadmapJourney extends ConsumerStatefulWidget {
  final Hobby hobby;
  final Set<String> completedStepIds;
  final String? defaultActiveStepId;
  const RoadmapJourney({required this.hobby, required this.completedStepIds, this.defaultActiveStepId});
}
```

From lib/screens/home/paused_hobby_page.dart:
```dart
class PausedHobbyPage extends ConsumerWidget {
  final UserHobby userHobby;
  final VoidCallback? onResume;
  const PausedHobbyPage({super.key, required this.userHobby, this.onResume});
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract JournalEntryTile and ActiveHobbyPage into standalone files</name>
  <files>lib/screens/home/home_journal_section.dart, lib/screens/home/active_hobby_page.dart, lib/screens/home/home_screen.dart</files>
  <action>
This is a PURE REFACTOR. Zero logic changes, zero styling changes. Move code exactly as-is, only changing visibility.

**Step A — Create `lib/screens/home/home_journal_section.dart`:**
1. Move `_JournalEntryTile` (lines ~1136-1206 in current home_screen.dart after Plan 01 edits) into new file as `JournalEntryTile` (public).
2. Change class name from `_JournalEntryTile` to `JournalEntryTile`. Remove underscore from constructor.
3. Copy necessary imports:
   - `package:flutter/material.dart`
   - `package:cached_network_image/cached_network_image.dart`
   - `package:material_design_icons_flutter/material_design_icons_flutter.dart`
   - `../../components/glass_card.dart`
   - `../../theme/app_colors.dart`
   - `../../theme/app_typography.dart`
4. Do NOT change any widget tree, colors, padding, text, or behavior.

**Step B — Create `lib/screens/home/active_hobby_page.dart`:**
1. Move `_HobbyPageContent` + `_HobbyPageContentState` (lines ~492-1051 after Plan 01 edits) into new file as `ActiveHobbyPage` + `_ActiveHobbyPageState`.
2. Also move `_RestartCard` (lines ~1057-1130 after Plan 01 edits) into the same file — keep it private `_RestartCard` since it's only used by ActiveHobbyPage.
3. Rename `_HobbyPageContent` to `ActiveHobbyPage`. Rename `_HobbyPageContentState` to `_ActiveHobbyPageState`. Update `ConsumerState<_HobbyPageContent>` to `ConsumerState<ActiveHobbyPage>`.
4. Copy necessary imports:
   - `package:flutter/material.dart`
   - `package:flutter_riverpod/flutter_riverpod.dart`
   - `package:go_router/go_router.dart`
   - `package:cached_network_image/cached_network_image.dart`
   - `package:material_design_icons_flutter/material_design_icons_flutter.dart`
   - `../../components/app_overlays.dart` (for showAppSheet)
   - `../../components/glass_card.dart`
   - `../../components/hobby_quick_links.dart`
   - `../../components/plan_first_session_card.dart`
   - `../../components/starter_kit_card.dart`
   - `../../models/hobby.dart`
   - `../../providers/hobby_provider.dart`
   - `../../providers/subscription_provider.dart`
   - `../../providers/user_provider.dart`
   - `../../theme/app_colors.dart`
   - `../../theme/app_icons.dart`
   - `../../theme/app_typography.dart`
   - `../../theme/spacing.dart`
   - `home_journal_section.dart` (for JournalEntryTile)
   - `home_roadmap_section.dart` (for RoadmapJourney, from Plan 01)
5. Update internal references: `_JournalEntryTile(` -> `JournalEntryTile(` and `_RoadmapJourney(` -> `RoadmapJourney(` (the latter should already be updated from Plan 01).
6. Do NOT change any widget tree, colors, padding, text, or behavior.

**Step C — Update `lib/screens/home/home_screen.dart`:**
1. Remove the extracted code blocks:
   - `_HobbyPageContent` + `_HobbyPageContentState` (including `_showStopConfirmation` and `_showPauseConfirmation` methods)
   - `_RestartCard`
   - `_JournalEntryTile`
2. Add imports for the new files:
   - `import 'active_hobby_page.dart';`
   - `import 'home_journal_section.dart';` (only if JournalEntryTile is referenced directly in home_screen — check; it might only be used inside ActiveHobbyPage)
3. Update references:
   - `_HobbyPageContent(` -> `ActiveHobbyPage(`
4. Remove imports that are no longer used by home_screen.dart. After all extractions, home_screen.dart should only need:
   - `package:flutter/material.dart`
   - `package:flutter_riverpod/flutter_riverpod.dart`
   - `package:go_router/go_router.dart`
   - `../../components/app_background.dart`
   - `../../components/glass_card.dart`
   - `../../components/logo_loader.dart`
   - `../../components/page_dots.dart`
   - `../../models/hobby.dart`
   - `../../providers/hobby_provider.dart`
   - `../../providers/subscription_provider.dart`
   - `../../providers/user_provider.dart`
   - `../../theme/app_colors.dart`
   - `../../theme/app_typography.dart`
   - `paused_hobby_page.dart`
   - `active_hobby_page.dart`
   - Plus: `package:material_design_icons_flutter/material_design_icons_flutter.dart` if `_EmptyHomeState` uses MdiIcons
5. Check each remaining import — only keep what home_screen.dart still uses directly.
6. `_EmptyHomeState` stays in home_screen.dart (only 49 lines, simple, only used here).
7. `_ProLockedOverlay` stays in home_screen.dart (only 70 lines, simple, only used here).
8. `_HobbyPage` stays in home_screen.dart (only 28 lines, thin async loader).
  </action>
  <verify>
    <automated>cd /c/dev/trysomething && dart analyze lib/screens/home/</automated>
  </verify>
  <done>
    - `active_hobby_page.dart` exists with public `ActiveHobbyPage` class + private `_RestartCard`, identical widget trees to originals
    - `home_journal_section.dart` exists with public `JournalEntryTile` class, identical widget tree to original `_JournalEntryTile`
    - `home_screen.dart` imports both new files and references `ActiveHobbyPage`
    - `dart analyze lib/screens/home/` passes with 0 errors, 0 warnings
    - No logic, styling, or behavioral changes
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify final line count and full project analysis</name>
  <files>lib/screens/home/home_screen.dart</files>
  <action>
1. Run `wc -l lib/screens/home/*.dart` to confirm:
   - `home_screen.dart` is under 500 lines (target ~360-400 lines)
   - All extracted files exist and have reasonable sizes
2. Run `dart analyze lib/screens/home/` to verify the entire directory passes with 0 errors, 0 warnings.
3. Run `dart analyze lib/` as a broader check that no other file in the project was broken by the extractions (other files import from `home_screen.dart` — but since we only made private classes public and didn't change any public API, this should pass).
4. If home_screen.dart is still over 500 lines, identify what else can be moved. The `_EmptyHomeState` (49 lines) and `_ProLockedOverlay` (70 lines) can be extracted if needed, but they should not be necessary given the math.
5. If any analyze errors exist elsewhere in the project, fix import paths. The only risk is if another file was importing a symbol that moved — but all moved classes were private (underscore-prefixed), so they could NOT have been imported externally. This step should be a no-op.
  </action>
  <verify>
    <automated>cd /c/dev/trysomething && wc -l lib/screens/home/*.dart && dart analyze lib/screens/home/ && dart analyze lib/</automated>
  </verify>
  <done>
    - `home_screen.dart` is under 500 lines (HOME-01 requirement met)
    - `dart analyze lib/screens/home/` — 0 errors, 0 warnings
    - `dart analyze lib/` — 0 errors, 0 warnings (no breakage across the project)
    - 5 files exist in `lib/screens/home/`: home_screen.dart, paused_hobby_page.dart, home_roadmap_section.dart, active_hobby_page.dart, home_journal_section.dart
  </done>
</task>

</tasks>

<verification>
1. `wc -l lib/screens/home/home_screen.dart` — under 500 lines
2. `dart analyze lib/screens/home/` — 0 errors, 0 warnings
3. `dart analyze lib/` — 0 errors, 0 warnings (full project)
4. `ls lib/screens/home/*.dart | wc -l` — 5 files
5. All extracted widgets maintain identical class structure, widget trees, and behavior
</verification>

<success_criteria>
- home_screen.dart is under 500 lines, containing only PageView shell, loading/empty states, and widget composition (HOME-01)
- ActiveHobbyPage is a standalone public widget with identical rendering to _HobbyPageContent (HOME-03)
- JournalEntryTile is a standalone public widget with identical rendering to _JournalEntryTile (HOME-04)
- dart analyze passes with 0 errors, 0 warnings across the entire project
- Zero visual or behavioral changes — pure file extraction refactor
</success_criteria>

<output>
After completion, create `.planning/phases/15-home-screen-refactor/15-02-SUMMARY.md`
</output>
