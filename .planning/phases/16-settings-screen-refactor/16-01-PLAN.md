---
phase: 16-settings-screen-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/screens/settings/settings_screen.dart
  - lib/screens/settings/edit_profile_sheet.dart
  - lib/components/photo_picker_overlay.dart
autonomous: true
requirements:
  - SETT-02
  - SETT-03

must_haves:
  truths:
    - "Edit profile bottom sheet opens, validates, and saves profile changes identically -- name, bio, avatar picker, save button all unchanged"
    - "Photo picker overlay shows camera and gallery options centered on screen with dismiss layer, identical styling and behavior"
    - "Photo picker overlay is in lib/components/ and can be imported by any screen -- it is not settings-specific"
  artifacts:
    - path: "lib/screens/settings/edit_profile_sheet.dart"
      provides: "EditProfileSheet widget + FieldLabel + SheetTextField + ProfileInitials extracted from settings_screen.dart"
      contains: "class EditProfileSheet"
    - path: "lib/components/photo_picker_overlay.dart"
      provides: "Shared PhotoPickerOverlay + PhotoPickerOption widgets usable by any screen"
      contains: "class PhotoPickerOverlay"
    - path: "lib/screens/settings/settings_screen.dart"
      provides: "Settings screen now importing extracted widgets instead of defining them inline"
  key_links:
    - from: "lib/screens/settings/edit_profile_sheet.dart"
      to: "lib/components/photo_picker_overlay.dart"
      via: "import and PhotoPickerOverlay constructor in _showPhotoPickerMenu"
      pattern: "PhotoPickerOverlay\\("
    - from: "lib/screens/settings/settings_screen.dart"
      to: "lib/screens/settings/edit_profile_sheet.dart"
      via: "import and EditProfileSheet constructor in _showEditProfileSheet"
      pattern: "EditProfileSheet\\("
---

<objective>
Extract the edit profile bottom sheet and photo picker overlay from settings_screen.dart into standalone files.

Purpose: First extraction pass -- move the edit profile sheet (~460 lines including FieldLabel, SheetTextField, ProfileInitials) and the photo picker overlay (~110 lines) out of settings_screen.dart. The photo picker becomes a shared reusable component in `lib/components/` for use by journal, coach, and settings screens (Phase 20 will wire up the other consumers).

Output: Two new files (`edit_profile_sheet.dart`, `photo_picker_overlay.dart`) and a slimmed settings_screen.dart that imports them.
</objective>

<execution_context>
@C:/Users/rmulororiz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rmulororiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/screens/settings/settings_screen.dart

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/screens/settings/settings_screen.dart (widgets to extract):
- `_EditProfileSheet` (lines 1245-1623) -- StatefulWidget with WidgetRef, name/bio/avatar editing, photo upload
- `_EditProfileSheetState` (lines 1264-1623) -- State class with TextEditingControllers, _pickAndUpload, _save
- `_FieldLabel` (lines 1626-1641) -- StatelessWidget, uppercased label text
- `_SheetTextField` (lines 1643-1683) -- StatelessWidget, styled TextField wrapper
- `_ProfileInitials` (lines 1685-1701) -- StatelessWidget, initial letter fallback for avatar
- `_SettingsPhotoPickerOverlay` (lines 1974-2038) -- StatelessWidget, fullscreen overlay with dismiss + centered menu
- `_SettingsPickerOption` (lines 2041-2081) -- StatelessWidget, single option row in picker menu

From lib/core/media/image_upload.dart (used by edit profile):
```dart
class ImageUpload {
  static Future<String?> moderateAndUpload(File file);
}
class ImageModerationException implements Exception {
  final String reason;
}
```

From lib/components/app_overlays.dart (used by edit profile for snackbar):
```dart
void showAppSnackbar(BuildContext context, {required String message, required AppSnackbarType type});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PhotoPickerOverlay to shared component</name>
  <files>lib/components/photo_picker_overlay.dart, lib/screens/settings/settings_screen.dart</files>
  <action>
This is a PURE REFACTOR. Zero logic changes, zero styling changes. Move code exactly as-is, only changing visibility (remove underscore prefix to make classes public).

**Step A -- Create `lib/components/photo_picker_overlay.dart`:**
1. Move `_SettingsPhotoPickerOverlay` (lines 1974-2038) into new file as `PhotoPickerOverlay` (public class, renamed from settings-specific to generic).
2. Move `_SettingsPickerOption` (lines 2041-2081) into same file as `PhotoPickerOption` (public class).
3. Rename:
   - `_SettingsPhotoPickerOverlay` -> `PhotoPickerOverlay`
   - `_SettingsPickerOption` -> `PhotoPickerOption`
4. Keep the exact same constructor parameters: `onCamera`, `onGallery`, `onDismiss` for the overlay; `icon`, `label`, `onTap`, `isFirst`, `isLast` for the option.
5. Add necessary imports:
   - `package:flutter/material.dart`
   - `../theme/app_colors.dart`
   - `../theme/app_typography.dart`
6. Do NOT change any widget tree, colors, padding, text, or behavior.

**Step B -- Update `lib/screens/settings/settings_screen.dart`:**
1. Remove the extracted code blocks (lines 1973-2082: `_SettingsPhotoPickerOverlay` and `_SettingsPickerOption`).
2. Add import: `import '../../components/photo_picker_overlay.dart';`
3. Update references in `_EditProfileSheetState._showPhotoPickerMenu` (line 1297):
   - `_SettingsPhotoPickerOverlay(` -> `PhotoPickerOverlay(`
4. The `_SettingsPickerOption` reference is only inside `_SettingsPhotoPickerOverlay` which is now in the new file, so no further reference updates needed in settings_screen.dart.
  </action>
  <verify>
    <automated>cd /c/dev/trysomething && dart analyze lib/components/photo_picker_overlay.dart lib/screens/settings/settings_screen.dart</automated>
  </verify>
  <done>
    - `photo_picker_overlay.dart` exists in `lib/components/` with public `PhotoPickerOverlay` and `PhotoPickerOption` classes
    - `settings_screen.dart` imports the new file and references `PhotoPickerOverlay` instead of `_SettingsPhotoPickerOverlay`
    - `dart analyze` passes with 0 errors, 0 warnings on both files
    - Zero visual or behavioral changes
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract EditProfileSheet and supporting widgets</name>
  <files>lib/screens/settings/edit_profile_sheet.dart, lib/screens/settings/settings_screen.dart</files>
  <action>
This is a PURE REFACTOR. Zero logic changes, zero styling changes. Move code exactly as-is, only changing visibility where needed.

**Step A -- Create `lib/screens/settings/edit_profile_sheet.dart`:**
1. Move these classes from settings_screen.dart into the new file:
   - `_EditProfileSheet` + `_EditProfileSheetState` (lines 1245-1623) -> rename to `EditProfileSheet` (public) and keep `_EditProfileSheetState` private
   - `_FieldLabel` (lines 1626-1641) -> keep private `_FieldLabel` (only used within this file)
   - `_SheetTextField` (lines 1643-1683) -> keep private `_SheetTextField` (only used within this file)
   - `_ProfileInitials` (lines 1685-1701) -> keep private `_ProfileInitials` (only used within this file)
2. Copy all necessary imports for the new file:
   - `dart:io` (for File)
   - `package:flutter/material.dart`
   - `package:flutter_riverpod/flutter_riverpod.dart`
   - `package:cached_network_image/cached_network_image.dart`
   - `package:image_picker/image_picker.dart`
   - `../../core/media/image_upload.dart`
   - `../../providers/auth_provider.dart`
   - `../../components/app_overlays.dart` (for showAppSnackbar)
   - `../../components/photo_picker_overlay.dart` (the newly extracted file from Task 1)
   - `../../theme/app_colors.dart`
   - `../../theme/app_typography.dart`
3. Do NOT change any widget tree, colors, padding, text, or behavior.

**Step B -- Update `lib/screens/settings/settings_screen.dart`:**
1. Remove the extracted code blocks:
   - Lines 1244-1701 (the `// ── Edit profile bottom sheet ──` comment through `_ProfileInitials`)
2. Add import: `import 'edit_profile_sheet.dart';`
3. Update reference in `_showEditProfileSheet` method (around line 891):
   - `_EditProfileSheet(` -> `EditProfileSheet(`
4. Remove imports that are no longer used by settings_screen.dart ONLY if they were exclusively used by the extracted widgets. Be conservative -- if unsure, leave the import. Specifically check:
   - `dart:io` -- was it only used by EditProfileSheet's `File(picked.path)`? If so, remove.
   - `package:image_picker/image_picker.dart` -- was it only used by EditProfileSheet? If so, remove.
   - `../../core/media/image_upload.dart` -- was it only used by EditProfileSheet? If so, remove.
   - `package:cached_network_image/cached_network_image.dart` -- check if _ProfileSection (still in settings_screen.dart) also uses it. _ProfileSection (line 1159) uses CachedNetworkImage, so KEEP this import.
   - `../../components/photo_picker_overlay.dart` -- remove from settings_screen.dart since it's only used inside EditProfileSheet now.
5. Run `dart analyze` on all three files to verify.
  </action>
  <verify>
    <automated>cd /c/dev/trysomething && dart analyze lib/screens/settings/edit_profile_sheet.dart lib/screens/settings/settings_screen.dart lib/components/photo_picker_overlay.dart</automated>
  </verify>
  <done>
    - `edit_profile_sheet.dart` exists with public `EditProfileSheet` class and private `_FieldLabel`, `_SheetTextField`, `_ProfileInitials`
    - `settings_screen.dart` imports `edit_profile_sheet.dart` and references `EditProfileSheet` instead of `_EditProfileSheet`
    - `dart analyze` passes with 0 errors, 0 warnings on all files
    - Edit profile sheet opens, validates, and saves identically -- same avatar picker, name/bio fields, save button
    - Zero visual or behavioral changes
  </done>
</task>

</tasks>

<verification>
1. `dart analyze lib/screens/settings/ lib/components/photo_picker_overlay.dart` -- 0 errors, 0 warnings
2. `wc -l lib/screens/settings/settings_screen.dart` -- file should be approximately 1,540 lines (removed ~460 for edit profile + ~110 for photo picker = ~570 lines removed from 2,082)
3. All extracted widgets maintain identical class structure and widget trees
</verification>

<success_criteria>
- EditProfileSheet is a standalone public widget in its own file with all supporting widgets (SETT-02)
- PhotoPickerOverlay is a shared reusable component in `lib/components/` importable by any screen (SETT-03)
- settings_screen.dart imports and uses the extracted widgets
- dart analyze passes with 0 errors, 0 warnings on all files
- Zero visual or behavioral changes
</success_criteria>

<output>
After completion, create `.planning/phases/16-settings-screen-refactor/16-01-SUMMARY.md`
</output>
