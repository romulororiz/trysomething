---
phase: 16-settings-screen-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - lib/screens/settings/settings_screen.dart
  - lib/screens/settings/settings_widgets.dart
autonomous: true
requirements:
  - SETT-04
  - SETT-01

must_haves:
  truths:
    - "Settings section labels, tiles, stepper buttons, budget selector, toggle chips, and debug toggle render identically to before -- same glass cards, same spacing, same colors"
    - "settings_screen.dart is under 500 lines and contains only the scaffold, section list layout, navigation handlers, dialog methods, and account deletion helpers"
    - "Every Settings interaction (edit profile, toggle, link tap, restore, logout, clear data, delete account) behaves identically to before"
  artifacts:
    - path: "lib/screens/settings/settings_widgets.dart"
      provides: "SectionLabel, SettingsTile, StepperButton, BudgetSelector, ToggleChip, DebugProToggle, ProfileSection extracted from settings_screen.dart"
      contains: "class SettingsTile"
    - path: "lib/screens/settings/settings_screen.dart"
      provides: "Lean settings screen under 500 lines importing extracted widgets"
  key_links:
    - from: "lib/screens/settings/settings_screen.dart"
      to: "lib/screens/settings/settings_widgets.dart"
      via: "import and usage of SectionLabel, SettingsTile, StepperButton, BudgetSelector, ToggleChip, DebugProToggle, ProfileSection"
      pattern: "SettingsTile\\(|SectionLabel\\(|ProfileSection\\("
---

<objective>
Extract settings section builder widgets and the profile section from settings_screen.dart into a shared widgets file, bringing settings_screen.dart under 500 lines.

Purpose: Second and final extraction pass -- move the reusable section builder widgets (~200 lines for SectionLabel, SettingsTile, StepperButton, BudgetSelector, ToggleChip), the DebugProToggle (~70 lines), and the ProfileSection display widget (~85 lines) into `settings_widgets.dart`. This brings settings_screen.dart to its target of under 500 lines.

Output: One new file (`settings_widgets.dart`) and a final slimmed settings_screen.dart.
</objective>

<execution_context>
@C:/Users/rmulororiz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rmulororiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-settings-screen-refactor/16-01-SUMMARY.md
@lib/screens/settings/settings_screen.dart

<interfaces>
<!-- Key types and contracts the executor needs. -->
<!-- After Plan 16-01, settings_screen.dart will be ~1,540 lines. -->
<!-- The following private widgets remain in settings_screen.dart and need extraction: -->

Widgets to extract to settings_widgets.dart (make public):
- `_ProfileSection` (~lines 1159-1242 in original, ~85 lines) -- StatelessWidget showing avatar + name + email display
- `_SectionLabel` (~lines 1707-1721 in original, ~15 lines) -- StatelessWidget, section header text
- `_SettingsTile` (~lines 1727-1784 in original, ~58 lines) -- StatelessWidget, GlassCard row with icon + title + subtitle + trailing
- `_StepperButton` (~lines 1790-1820 in original, ~31 lines) -- StatelessWidget, circular +/- button
- `_BudgetSelector` (~lines 1826-1860 in original, ~35 lines) -- StatelessWidget, 3 budget tier buttons
- `_ToggleChip` (~lines 1866-1899 in original, ~34 lines) -- StatelessWidget, on/off pill chip
- `_DebugProToggle` (~lines 1905-1971 in original, ~67 lines) -- ConsumerWidget, debug tier selector

Note: Line numbers reference the ORIGINAL file before Plan 16-01 extractions.
After Plan 16-01, these widgets will be at different line numbers but still present.
The executor should find them by class name, not line number.

From lib/components/glass_card.dart (used by SettingsTile and DebugProToggle):
```dart
class GlassCard extends StatelessWidget {
  final Widget child;
  final EdgeInsets? padding;
  final double borderRadius;
  final bool blur;
  final VoidCallback? onTap;
}
```

From lib/providers/subscription_provider.dart (used by DebugProToggle):
```dart
enum DebugTier { none, free, pro }
// proStatusProvider notifier exposes: debugTier, setDebugTier(tier)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract settings helper widgets and ProfileSection into settings_widgets.dart</name>
  <files>lib/screens/settings/settings_widgets.dart, lib/screens/settings/settings_screen.dart</files>
  <action>
This is a PURE REFACTOR. Zero logic changes, zero styling changes. Move code exactly as-is, only changing visibility (remove underscore prefix to make classes public).

**Step A -- Create `lib/screens/settings/settings_widgets.dart`:**
1. Move these classes from settings_screen.dart into the new file (find by class name, not line number, since Plan 16-01 already changed line numbers):
   - `_ProfileSection` -> `ProfileSection` (public)
   - `_SectionLabel` -> `SectionLabel` (public)
   - `_SettingsTile` -> `SettingsTile` (public)
   - `_StepperButton` -> `StepperButton` (public)
   - `_BudgetSelector` -> `BudgetSelector` (public)
   - `_ToggleChip` -> `ToggleChip` (public)
   - `_DebugProToggle` -> `DebugProToggle` (public)
2. Add necessary imports for the new file:
   - `package:flutter/material.dart`
   - `package:flutter_riverpod/flutter_riverpod.dart` (needed by DebugProToggle)
   - `package:cached_network_image/cached_network_image.dart` (needed by ProfileSection avatar)
   - `../../components/glass_card.dart` (needed by SettingsTile, DebugProToggle)
   - `../../providers/subscription_provider.dart` (needed by DebugProToggle for DebugTier, proStatusProvider)
   - `../../theme/app_colors.dart`
   - `../../theme/app_typography.dart`
   - `../../theme/motion.dart` (needed by BudgetSelector, ToggleChip, DebugProToggle AnimatedContainer)
3. Do NOT change any widget tree, colors, padding, text, or behavior.

**Step B -- Update `lib/screens/settings/settings_screen.dart`:**
1. Remove all extracted widget class definitions (find by class name since line numbers shifted from Plan 16-01).
2. Also remove the section separator comments above each extracted class (the `// ═══...` lines).
3. Add import: `import 'settings_widgets.dart';`
4. Update ALL references throughout the build method and rest of the file:
   - `_ProfileSection(` -> `ProfileSection(`
   - `_SectionLabel(` -> `SectionLabel(`
   - `_SettingsTile(` -> `SettingsTile(`
   - `_StepperButton(` -> `StepperButton(`
   - `_BudgetSelector(` -> `BudgetSelector(`
   - `_ToggleChip(` -> `ToggleChip(`
   - `_DebugProToggle()` -> `DebugProToggle()`
   - Also update any `const _SectionLabel(` -> `const SectionLabel(`
5. Remove imports that are no longer used by settings_screen.dart ONLY if they were exclusively used by the extracted widgets. Specifically check:
   - `../../components/glass_card.dart` -- check if settings_screen.dart still uses GlassCard directly in its build method (e.g., the debug section inline GlassCards for "Reset hobby data", "Sync from server", etc.). YES, it does -- KEEP this import.
   - `../../theme/motion.dart` -- check if settings_screen.dart still uses Motion directly. It uses `Motion.normal` for AnimatedOpacity on the "See updated matches" button. KEEP this import.
   - `../../providers/subscription_provider.dart` -- check if settings_screen.dart still uses proStatusProvider directly. YES, it does (line 228 `ref.watch(proStatusProvider).isPro`). KEEP this import.
   - `package:cached_network_image/cached_network_image.dart` -- was used by _ProfileSection which is now extracted. If settings_screen.dart no longer uses CachedNetworkImage directly, remove it.
6. Run `wc -l` to verify settings_screen.dart is under 500 lines.
  </action>
  <verify>
    <automated>cd /c/dev/trysomething && dart analyze lib/screens/settings/ && wc -l lib/screens/settings/*.dart</automated>
  </verify>
  <done>
    - `settings_widgets.dart` exists with public `ProfileSection`, `SectionLabel`, `SettingsTile`, `StepperButton`, `BudgetSelector`, `ToggleChip`, `DebugProToggle` classes
    - `settings_screen.dart` is under 500 lines
    - `settings_screen.dart` imports `settings_widgets.dart` and references all public widget names
    - `dart analyze lib/screens/settings/` passes with 0 errors, 0 warnings
    - Every Settings interaction behaves identically -- toggles, budget selector, stepper, section labels, profile display, debug toggle all unchanged
    - Zero visual or behavioral changes
  </done>
</task>

</tasks>

<verification>
1. `dart analyze lib/screens/settings/` -- 0 errors, 0 warnings
2. `wc -l lib/screens/settings/settings_screen.dart` -- under 500 lines
3. `wc -l lib/screens/settings/*.dart` -- total across all settings files approximately equals original 2,082 lines
4. All extracted widgets maintain identical class structure and widget trees
</verification>

<success_criteria>
- Settings helper widgets are in a standalone `settings_widgets.dart` file (SETT-04)
- `settings_screen.dart` is under 500 lines with only scaffold, section list, navigation handlers, dialog methods, and account deletion helpers (SETT-01)
- dart analyze passes with 0 errors, 0 warnings on all settings files
- Zero visual or behavioral changes
</success_criteria>

<output>
After completion, create `.planning/phases/16-settings-screen-refactor/16-02-SUMMARY.md`
</output>
