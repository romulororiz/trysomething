---
phase: quick
plan: 260321-s8z
type: execute
wave: 1
depends_on: []
files_modified:
  - server/vercel.json
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "POST /api/auth/apple routes to the auth handler and reaches handleApple()"
    - "Existing auth routes (register, login, refresh, google) remain unaffected"
  artifacts:
    - path: "server/vercel.json"
      provides: "Apple OAuth route matching"
      contains: "register|login|refresh|google|apple"
  key_links:
    - from: "server/vercel.json (line 11 route regex)"
      to: "server/api/auth/[action].ts (case 'apple')"
      via: "Vercel route rewrite with ?action=apple"
      pattern: "apple.*\\[action\\]"
---

<objective>
Add `|apple` to the auth action regex in `server/vercel.json` so that `POST /api/auth/apple` is routed to the existing `handleApple()` handler in `server/api/auth/[action].ts`.

Purpose: Apple Sign-In requests currently 404 because the Vercel route regex on line 11 only matches `(register|login|refresh|google)`, skipping `apple`. The handler code already exists and works — only the routing config is missing.

Output: One-line fix in `server/vercel.json`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@server/vercel.json
@server/api/auth/[action].ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add apple to auth route regex in vercel.json</name>
  <files>server/vercel.json</files>
  <action>
On line 11 of `server/vercel.json`, change the route regex from:

```
{ "src": "/api/auth/(register|login|refresh|google)", "dest": "/api/auth/[action].ts?action=$1" }
```

to:

```
{ "src": "/api/auth/(register|login|refresh|google|apple)", "dest": "/api/auth/[action].ts?action=$1" }
```

This is the ONLY change. Do not modify any other route or file. The handler `handleApple()` in `server/api/auth/[action].ts` already exists with the `case "apple"` switch entry (line 35).
  </action>
  <verify>
    <automated>cd server && node -e "const v = require('./vercel.json'); const authRoute = v.routes.find(r => r.src.includes('/api/auth/')); const re = new RegExp(authRoute.src); console.log('apple matches:', re.test('/api/auth/apple')); console.log('google matches:', re.test('/api/auth/google')); console.log('login matches:', re.test('/api/auth/login')); if (!re.test('/api/auth/apple')) process.exit(1);"</automated>
  </verify>
  <done>The regex `(register|login|refresh|google|apple)` in vercel.json line 11 matches `/api/auth/apple`, and all existing auth routes (register, login, refresh, google) still match.</done>
</task>

</tasks>

<verification>
1. `server/vercel.json` line 11 contains `(register|login|refresh|google|apple)`
2. No other lines in vercel.json changed
3. Existing server tests still pass: `cd server && npm test`
</verification>

<success_criteria>
- `/api/auth/apple` matches the Vercel route regex and rewrites to `[action].ts?action=apple`
- All other auth routes unaffected
- Server tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260321-s8z-apple-oauth-routing-fix/260321-s8z-SUMMARY.md`
</output>
