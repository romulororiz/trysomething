pre-commit:
  parallel: true
  commands:
    flutter-analyze:
      glob: "*.dart"
      run: dart analyze --no-fatal-warnings lib/
    typescript-typecheck:
      glob: "*.ts"
      root: "server/"
      run: ./node_modules/.bin/tsc --noEmit
